<?php namespace App\Console\Commands; use Illuminate\Console\Command; use App\Models\User; use App\Models\SalesPipeline; class TestScope extends Command { protected $signature = "test:scope"; public function handle() { $user = User::where("email", "ccare@demo.com")->first(); auth()->login($user); $count = SalesPipeline::where("ccare_id", $user->id)->count(); $this->info("Count is: " . $count); } }
