---
description: Push current changes to GitHub
---

1. Ensure all changes are staged or identified.
2. Run the `push.ps1` script or use `git` commands directly.
// turbo
3. Run the following command to push changes:
```powershell
./push.ps1 -Message "Project cleanup and GitHub integration setup"
```
4. Verify the push was successful by checking the output.
