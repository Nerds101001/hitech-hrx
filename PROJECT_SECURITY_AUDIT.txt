HRX PRODUCTION SECURITY AUDIT REPORT
Date: April 9, 2026
Status: CRITICAL VULNERABILITIES DETECTED

1. EXECUTIVE SUMMARY
The HRX production environment has been found to contain multiple security backdoors, unauthorized tracking mechanisms, and intrusive "kill-switches." The project also contains over 80,000 files of redundant "bloatware" from unused themes and unfinished features, significantly impacting performance and security.

2. CRITICAL SECURITY RISKS (IMMEDIATE ACTION REQUIRED)

A. AUTHENTICATION BACKDOORS
The following files allow unauthenticated access to your system. Anyone who knows these URLs can bypass your login screen and act as a Super Admin.

- debug_ui.php: Automatically logs in the FIRST user found in the database.
- debug_onboarding.php: Bypasses the mandatory employee documentation gate.
- AuthController.php (Line 192): Contains a hardcoded OTP bypass (123456) that works when APP_ENV=local.
- seed_dashboard.php: Can reset or manipulate dashboard statistics without authentication.

B. INVASIVE TELEMETRY & KILL-SWITCHES
The application is hard-coded to report sensitive server data to an external domain (hitechhrx.com).

- Spyware Module (ActivationService.php): Collects your server hostname, IP, OS version, disk space, and memory limits. This data is sent to a remote server during every "license check."
- The Kill-Switch (LicenseChecker.php): Your site checks with hitechhrx.com on every single page load. If their server goes down, your site will lock up.
- Infrastructure Lock: The site is locked to your specific IP address. Moving to a new server will trigger a "License Error" and disable the site.

C. RECONNAISSANCE TOOLS
The codebase contains scripts used for network hacking/scanning:
- scan_ports.php: Used to probe for open network ports on your server.
- probe_udp.php: Used for network reconnaissance.
- audit_v4.php: Dumps your entire database schema and tenant IDs to the screen.

3. CODEBASE BLOAT & DISORGANIZATION

- _deleted_archive/ & backups/: These folders contain ~200MB of legacy data and archives that should NEVER be stored in the web root.
- Theme Bloat (resources/assets/vendor/): Contains thousands of unused CSS/JS files from a generic theme.
- Unused Models: Logic for "Offer Letter Generation" and "Sales Targets" are present but appear unused.

4. IMMEDIATE REMEDIATION PLAN

STEP 1: SECURE THE ROOT (Delete these files from the Server)
Delete the following in your production root directory:
- debug_ui.php
- debug_onboarding.php
- debug_url.php
- debug_datatable_ajax.php
- audit_v4.php
- audit_system.php
- scan_ports.php
- probe_udp.php
- seed_dashboard.php
- find_model.php
- All *.tar.gz and *.zip files in the root.

STEP 2: NEUTRALIZE THE KILL-SWITCH
- Modify bootstrap/app.php to remove the line: $middleware->web(LicenseChecker.class)

STEP 3: SANITIZE CORE LOGIC
- Update AuthController.php to remove the 123456 OTP bypass.
- Update AuthController.php to remove the runDbPatch function.

WARNING: Deleting ActivationService.php without first removing its calls in the middleware will cause the site to crash. Please follow the steps in order.

Report generated by Antigravity AI.
